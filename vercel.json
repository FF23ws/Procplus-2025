{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "dist",
  "git": { "deployOnPush": true }
}
{
  "version": 2,
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "dist",
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}